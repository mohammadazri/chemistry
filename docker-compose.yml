version: '3.8'

services:
  db:
    image: postgres:15
    environment:
      POSTGRES_USER: hololab
      POSTGRES_PASSWORD: hololab123
      POSTGRES_DB: hololab_dev
    ports:
      - '5432:5432'
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
